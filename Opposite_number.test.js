var chai = require('chai');
var assert = chai.assert;
var Opposite_number = require('./Opposite_number.js');

describe('Opposite_number', function(){
  it('should return opposite number',function(){
      assert.equal(opposite(1), -1,)
  })
})
